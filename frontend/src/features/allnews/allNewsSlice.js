import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const allNewsSlice = createSlice({
    name: "news",
    initialState,
    reducers: {},
});

export const {} = allNewsSlice.actions;
export default allNewsSlice.reducer;
